<?xml version="1.0" encoding="UTF-8"?>
<GTUTEMPLATES xmlns="http://www.opentrafficsim.org/ots" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.opentrafficsim.org/ots ../ots-definitions.xsd
  http://www.opentrafficsim.org/ots https://opentrafficsim.org/docs/xsd/1.01.02/ots-definitions.xsd">

<!--
      // GTUType.CAR
      // from "Maatgevende normen in de Nederlandse richtlijnen voor wegontwerp", R-2014-38, SWOV
      template = new TemplateGTUType(type, new ConstantGenerator<>(Length.createSI(4.19)),
              new ConstantGenerator<>(Length.createSI(1.7)),
              new ConstantGenerator<>(new Speed(180, SpeedUnit.KM_PER_HOUR)));

      // GTUType.TRUCK
      // from "Maatgevende normen in de Nederlandse richtlijnen voor wegontwerp", R-2014-38, SWOV
      template = new TemplateGTUType(type, new ConstantGenerator<>(Length.createSI(12.0)),
              new ConstantGenerator<>(Length.createSI(2.55)),
              new ContinuousDistSpeed(new DistNormal(randomStream, 85.0, 2.5), SpeedUnit.KM_PER_HOUR));
            
      // GTUType.BUS
      template = new TemplateGTUType(type, new ConstantGenerator<>(Length.createSI(12.0)),
              new ConstantGenerator<>(Length.createSI(2.55)),
              new ConstantGenerator<>(new Speed(90, SpeedUnit.KM_PER_HOUR)));

      // GTUType.VAN
      template = new TemplateGTUType(type, new ConstantGenerator<>(Length.createSI(5.0)),
              new ConstantGenerator<>(Length.createSI(2.4)),
              new ConstantGenerator<>(new Speed(180, SpeedUnit.KM_PER_HOUR)));

      // EMERGENCY_VEHICLE
      template = new TemplateGTUType(type, new ConstantGenerator<>(Length.createSI(5.0)),
              new ConstantGenerator<>(Length.createSI(2.55)),
              new ConstantGenerator<>(new Speed(180, SpeedUnit.KM_PER_HOUR)));
 -->

  <GTUTEMPLATE NAME="CAR" GTUTYPE="CAR" DEFAULT="true" LENGTHDIST="CONST(4.19) m" WIDTHDIST="CONST(1.7) m"
    MAXSPEEDDIST="CONST(180) km/h" />
  <GTUTEMPLATE NAME="TRUCK" GTUTYPE="TRUCK" DEFAULT="true" LENGTHDIST="CONST(12.0) m" WIDTHDIST="CONST(2.55) m"
    MAXSPEEDDIST="NORMAL(85.0, 2.5) km/h" />
  <GTUTEMPLATE NAME="BUS" GTUTYPE="BUS" DEFAULT="true" LENGTHDIST="CONST(12.0) m" WIDTHDIST="CONST(2.55) m"
    MAXSPEEDDIST="CONST(90) km/h" />
  <GTUTEMPLATE NAME="VAN" GTUTYPE="VAN" DEFAULT="true" LENGTHDIST="CONST(5.0) m" WIDTHDIST="CONST(2.4) m" 
    MAXSPEEDDIST="CONST(180) km/h" />
  <GTUTEMPLATE NAME="EMERGENCY_VEHICLE" GTUTYPE="EMERGENCY_VEHICLE" DEFAULT="true" LENGTHDIST="CONST(5.0) m"
    WIDTHDIST="CONST(2.55) m" MAXSPEEDDIST="CONST(180) km/h" />

</GTUTEMPLATES>
